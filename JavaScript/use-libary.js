/**
 * export (module.export) 
 * import (require)
 */

// const [, times] = require('./libary.js')
const { times } = require('./libary.js')
// const [sum, times] = data

console.log(times(3, 5, 5, 3, 2, 2, 2, 6));